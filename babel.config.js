module.exports = {
  presets: ['next/babel'],
}

/*
Configurando o arquivo config do babel:

Dentro de module.exports = {} inserir a propriedade 'presets'

A propriedade 'presets' recebe 'next/babel'

O 'next/babel' é usado, pois a aplicação foi construída com o next, se fosse um projeto que não usasse next, poderia inserir a framework utilizada no projeto (babel-preset-env ou babel-preset-react)

Agora iniciar o primeiro teste unitário dentro do componente ActiveLink

O primeiro passo é criar um arquivo de teste para o componente chamado de 'ActiveLink.spec.ts'

Ir para o arquivo 'ActiveLink.spec.ts'
*/
